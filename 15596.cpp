#include <bits/stdc++.h>
using namespace std;

long long sum(std::vector<int> &a) {
	long long ans = 0;
	for (const auto & d : a)
	{
		ans += d;
	}
	return ans;
}

int main()
{
	ios::sync_with_stdio(0), cin.tie(0);

	return 0;
}